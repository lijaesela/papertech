/* limits on how big variable names and contents can be */
#define MAX_VARNAME_SIZE 69
#define MAX_VALUE_SIZE 420

/* the escape character that ptech uses if not given as an argument */
#define DEFAULT_ESC '\\'
